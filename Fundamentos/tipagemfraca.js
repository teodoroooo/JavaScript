let qualquer = 'legal'

console.log(qualquer)
console.log(typeof qualquer)

qualquer = 3.1516

console.log(qualquer)
console.log(typeof qualquer)
const nome = "Gabriel" //evitar usar var, pois é mais antiga e pode ser substituída por outra linguagem

let  age = "25" //Let permite que a variável seja mudada dentro do bloco de código onde foi declarada
                //Evita conflito entre as variáveis no escopo global e nas locais

let valor = ''
let numero = 1
let  pqp = `Meu ${nome} tem ${idade} anos`
