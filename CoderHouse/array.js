// Declarando array

const arrayA = [];

console.log(arrayA);

const arrayNumeros = [1, 2, 3];

console.log(arrayNumeros[0])

const arrayString = ['A', 'B', 'C'];

const arrayBooleano = [true, false];

const arrayMisto = [true, 'A', 3, 'B']

// Indice é a posição do elemento


for (let i = 0; i < 5; i++) {
    console.log(arrayNumeros[i])
}